$i18n.getText("admin.workflow.condition.subtaskblocking.view")
#foreach ($status in $statuses)<b>$textutils.htmlEncode($status.getString("name"), false)</b>#if($velocityCount != $statuses.size())#if($velocityCount == ($statuses.size() - 1))&nbsp;or&nbsp;#else,&nbsp;#end#else.#end#end